<Window x:Class="TaskCopy.Views.ConfirmDeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TaskCopy"
        Width="420"
        Height="244"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Mocha.Base.Brush}"
        Foreground="{DynamicResource Mocha.Text.Brush}"
        FontFamily="{DynamicResource Mocha.Font}"
        FontSize="13"
        UseLayoutRounding="True"
        ShowInTaskbar="False">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="PromptText"
                   Style="{DynamicResource Mocha.Body.Section}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1"
                Style="{DynamicResource Mocha.Panel}"
                Padding="12,10"
                Margin="0,10,0,0">
            <TextBlock Text="The snippet moves to Trash first. You can restore it for 30 days before TaskCopy purges it automatically."
                       Style="{DynamicResource Mocha.Body.Caption}" />
        </Border>

        <CheckBox Grid.Row="3"
                  x:Name="DontAskAgainBox"
                  Style="{DynamicResource Mocha.CheckBox}"
                  Content="Don't ask again for recoverable deletes"
                  Margin="0,12,0,12" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Style="{DynamicResource Mocha.Button}"
                    Content="Cancel"
                    Width="92"
                    IsCancel="True"
                    Click="OnCancel" />
            <Button Style="{DynamicResource Mocha.Button.Danger}"
                    Content="Delete"
                    Width="92"
                    Margin="8,0,0,0"
                    IsDefault="True"
                    Click="OnDelete" />
        </StackPanel>
    </Grid>
</Window>
