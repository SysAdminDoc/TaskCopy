<Window x:Class="TaskCopy.Views.AskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TaskCopy"
        Width="360"
        Height="180"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Mocha.Base.Brush}"
        Foreground="{StaticResource Mocha.Text.Brush}"
        FontFamily="{StaticResource Mocha.Font}"
        FontSize="13"
        UseLayoutRounding="True"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="PromptLabel"
                   Style="{StaticResource Mocha.Body.Header}"
                   Margin="0,0,0,8" />

        <TextBox Grid.Row="1"
                 x:Name="ValueBox"
                 Style="{StaticResource Mocha.TextBox}"
                 KeyDown="OnKeyDown" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Style="{StaticResource Mocha.Button}"
                    Content="Cancel"
                    Width="80"
                    IsCancel="True"
                    Click="OnCancel" />
            <Button Style="{StaticResource Mocha.Button.Accent}"
                    Content="OK"
                    Width="80"
                    Margin="8,0,0,0"
                    IsDefault="True"
                    Click="OnOk" />
        </StackPanel>
    </Grid>
</Window>
