<Window x:Class="TaskCopy.Views.FormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TaskCopy"
        Width="440"
        SizeToContent="Height"
        MinHeight="180"
        MaxHeight="560"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Mocha.Base.Brush}"
        Foreground="{DynamicResource Mocha.Text.Brush}"
        FontFamily="{DynamicResource Mocha.Font}"
        FontSize="13"
        UseLayoutRounding="True"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Snippet fields"
                   Style="{DynamicResource Mocha.Body.Title}"
                   Margin="0,0,0,4" />
        <TextBlock Grid.Row="1"
                   Text="Fill the values this snippet needs before paste."
                   Style="{DynamicResource Mocha.Body.Caption}"
                   Margin="0,0,0,12" />

        <ScrollViewer Grid.Row="2"
                      Margin="0,14,0,0"
                      MaxHeight="360"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="FieldsPanel" />
        </ScrollViewer>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Style="{DynamicResource Mocha.Button}"
                    Content="Cancel"
                    Width="80"
                    IsCancel="True"
                    Click="OnCancel" />
            <Button Style="{DynamicResource Mocha.Button.Accent}"
                    Content="OK"
                    Width="80"
                    Margin="8,0,0,0"
                    IsDefault="True"
                    Click="OnOk" />
        </StackPanel>
    </Grid>
</Window>
